# dependencies / scratch
node_modules/
.verify/
logs/
.agent-teams/
docs/

# npm
*.tgz
npm-debug.log*

# build output (regenerated by `npm run build`; `prepare` rebuilds on install /
# pack / publish, so committed artifacts are unnecessary and drift-prone)
lib/

# OS / editor
.DS_Store
*.swp